# Specification

## Summary
**Goal:** Update the app footer credit text to display “V1.0” consistently across the application.

**Planned changes:**
- Replace the footer text “Built by Hannes” with “V1.0” in the specified footer text element.
- Apply the same footer text update in all layouts where this footer is rendered (including the normal app layout and the access-denied layout).

**User-visible outcome:** Any page that shows the app footer (including the access-denied screen) displays exactly “V1.0”, and “Built by Hannes” is no longer shown anywhere.
