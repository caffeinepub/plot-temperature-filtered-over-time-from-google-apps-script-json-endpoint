{
  "kind": "build_request",
  "title": "Make 'CO2 Right (%)' a lighter green than 'CO2 Left (%)' in the CO2 chart (line + legend)",
  "priority": "normal",
  "requirements": [
    {
      "id": "REQ-1",
      "text": "Update the existing CO₂ chart so the 'CO2 Right (%)' series is rendered in a lighter green than the 'CO2 Left (%)' series, and ensure the legend swatch matches those same colors.",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-1"
        ],
        "quotes": [
          "kun je ook CO2 Right (%) een lichter groene kleur geven dan CO2 Left (%) (donkergroen)? in legende en grafiek zelf"
        ]
      },
      "acceptanceCriteria": [
        "In the CO2 chart, the 'CO2 Left (%)' line is a darker green than the 'CO2 Right (%)' line.",
        "The legend line icon for 'CO2 Left (%)' uses the same darker green as the chart line.",
        "The legend line icon for 'CO2 Right (%)' uses the same lighter green as the chart line.",
        "The color relationship (Right lighter than Left) is consistent in both light mode and dark mode."
      ]
    }
  ],
  "constraints": [
    "Do not edit any files under frontend/src/components/ui (read-only).",
    "Do not edit files listed in frontend immutablePaths (e.g., frontend/src/hooks/useInternetIdentity.ts(x), frontend/src/hooks/useActor.ts, frontend/src/main.tsx).",
    "Keep the backend single-actor architecture; no backend changes are required for this UI-only update."
  ],
  "nonGoals": [
    "Do not change any CO2 data fetching/parsing logic.",
    "Do not change chart series names/labels or add/remove series.",
    "Do not address the build/deploy pipeline 'Configuration error encountered. Please reach out to the Caffeine team.' as part of this change."
  ],
  "imageRequirements": {
    "required": [],
    "edits": []
  },
  "userProfileUpdate": {
    "goalsToAdd": [],
    "goalsToRemove": [],
    "preferencesToAdd": [],
    "preferencesToRemove": [],
    "miscToAdd": [],
    "miscToRemove": [],
    "fieldsToSet": {},
    "fieldsToDelete": []
  }
}